import { JobRoadmap } from "@/types/roadmap";
import { uiuxRoadmap } from "../uiux";
import { logoDesignerRoadmap } from "../logo-designer";

export const designRoadmaps: Record<string, JobRoadmap> = {
  "uxui-designer": uiuxRoadmap,
  "logo-designer": logoDesignerRoadmap,
};